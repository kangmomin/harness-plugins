---
name: default-conventions
description: "be-harness 범용 개발 가이드라인 (컨벤션 참조 문서). convention-check가 검사 기준으로 로드하며, 코드 작성 원칙이 궁금할 때도 사용. 프로젝트별 세부 컨벤션은 CLAUDE.md 또는 profile의 projectConventions에서 로드한다."
user-invocable: true
---

> **Project Overrides**: 실행 전 `.claude/be-harness/common.md`와 `.claude/be-harness/skills/default-conventions.md`를 Read.
> 존재하면 추가 규칙/예외로 흡수하고 충돌 시 오버라이드가 우선한다. 상세 규약: 플러그인 루트 `OVERRIDES.md`.


# be-harness 범용 개발 가이드라인

be-harness는 **언어/프레임워크에 중립적**이다. 이 문서는 어느 프로젝트에도 통용되는 최소한의 가이드라인만 정의한다.

프로젝트 특화 컨벤션(에러 처리, ORM 패턴, DI 등)은 **프로젝트 루트의 `CLAUDE.md`** 또는 profile의 `projectConventions`에 선언된 문서를 따른다.

---

## 1. 변경 범위 최소화 (Surgical Changes)

- 요청받은 변경만 수행한다. 무관한 리팩토링/스타일 개선 금지.
- 본인의 변경으로 생긴 미사용 코드(import, 변수)만 제거한다.
- 한 커밋은 하나의 책임(Single Responsibility)만 가진다.

## 2. Assumption 태그 의무

Spec/요청에 명시되지 않은 동작 변경(필터 추가, 정렬 변경, 상태 체크 추가 등)을 수행할 때는 **반드시** `[Assumption]` 태그로 기록한다.

- 코드 주석: `// [Assumption] ~ 이유`
- 커밋 메시지 본문
- 워크플로우 보고서

태그가 없으면 `convention-check` 가 위반으로 본다.

**수명주기**: 태그는 로컬 개발 단계의 기록이다. push/PR 시점에는 Assumption Gate(`/common:commit-push` Step 3)에서 항목별 사용자 확인을 거쳐 **모두 제거**되어야 하며(제거 시 `[확정]` 등 다른 태그로 치환하지 않고 태그만 삭제한다), 승인된 항목은 PR 본문의 "확정된 결정" 섹션(또는 최종 보고)으로 이관한다. 태그가 남아 있는 동안 push/PR은 차단된다.

## 3. 에러/로깅 일관성

- 새로 추가한 에러 처리는 **프로젝트 기존 패턴을 따른다**. 새 패턴을 혼자 도입하지 않는다.
- 로그 포맷/레벨도 기존과 일치시킨다.
- 프로젝트의 에러 코드/응답 포맷이 있으면 재사용한다 (새로 정의 금지).

> 프로젝트의 정확한 패턴은 `CLAUDE.md` 또는 유사 기능 구현체를 참조.

## 4. 입력 검증 및 경계

- 외부(HTTP, gRPC, 메시지 큐, CLI) 경계에서만 입력 검증을 수행한다.
- 내부 함수 간 방어적 nil check, 중복 검증은 피한다 (타입과 계약으로 보장).
- 프로젝트에 VO/Command 계층이 있으면 그 생성자에서만 검증한다.

## 5. 트랜잭션 / 리소스

- 트랜잭션, 파일 핸들, DB 커넥션 등 리소스는 획득한 스코프에서 책임지고 해제한다.
- 실패 경로도 항상 리소스를 해제하는지 확인한다.
- 프로젝트에 Unit of Work, context, defer 등 기존 패턴이 있으면 그대로 따른다.

## 6. 네이밍/스타일

- 프로젝트의 린터/포매터 결과를 그대로 수용한다 (`lintCommand` / `typeCheckCommand` 통과).
- 함수/변수명은 기존 모듈의 스타일을 추종한다.
- public API(exported symbol)에는 해당 언어의 문서 주석 규약을 따른다.

## 7. 쿼리/데이터 접근

- 동일 기능에 대한 참조 구현이 있으면 그 쿼리 패턴을 따른다.
- 성능 민감한 경로에서 N+1/불필요한 조회를 만들지 않는다.
- SQL 비교 연산:
  | 패턴 | 사용 기준 |
  |------|----------|
  | `WHERE x = ?` | 단일 값 비교 |
  | `WHERE x IN (?, ?, ...)` | 열거 가능한 다중 값 |
  | `WHERE x <= ?` 등 | 범위 비교 |

  단일 값에 `IN`을 쓰거나, 범위 비교에 `IN`을 쓰는 건 위반.

## 8. 외부 의존 추가

- 프로젝트에 없던 새 외부 패키지 import 추가 시, **의도적 도입인지 자동 확인**한다 (`[NEW_DEPENDENCY]` 태그).
- 기존에 유사 기능 패키지가 있으면 그것을 재사용.

## 9. 커밋 메시지

profile의 `commitPrefixes`를 사용한다.
커밋 메시지의 설명과 본문은 기본적으로 한국어로 작성하고, Prefix는 영문으로 유지한다.

```
<Prefix>: 간단한 설명

(본문: 왜 변경했는가 / 영향 / 테스트 결과)
```

기본 Prefix: `Add / Fix / Del / Refactor / Doc / Test / Chore / WIP`.

## 10. 상태값·열거값 표현

열거형 값의 표현은 **그 값이 어디서 왔는지**가 정한다. 출처가 다른 두 종류를 같은 형태로 쓰지 않는다.

| 값의 출처 | 표현 | 이유 |
|---|---|---|
| **외부 계약** — proto/gRPC enum, 공개 API 스키마 | 전용 타입 + 상수 (언어의 enum 또는 named type) | 계약이 값 집합을 소유하고, `UNSPECIFIED` 같은 계약 고유 규약이 딸려 온다 |
| **DB 제약** — `varchar` + `CHECK`, lookup 테이블 | 평범한 문자열 상수 집합 | DB 가 값 집합을 소유한다. 전용 타입을 씌우면 ORM 스캔·쿼리 바인드·DTO 경계마다 변환이 붙는다 |

DB 컬럼이 네이티브 enum 타입인지 `varchar` + `CHECK` 인지부터 확인한다 — 둘은 다르고, 마이그레이션을 보면 즉시 갈린다.

원칙:

- **값을 리터럴로 흩지 않는다.** 도메인 레이어 한 곳에 상수로 모으고 나머지는 참조한다. 허용값 검증 목록도 그 상수를 키로 쓴다 — 상수 블록과 검증 목록이 각자 리터럴을 들면 한쪽만 늘어난다.
- **일부만 상수화하지 않는다.** 5값 중 하나만 뽑으면 "왜 이것만?" 이라는 비대칭이 남고, 나머지를 쓰는 코드는 리터럴로 되돌아간다. 하나가 필요해지면 그 집합 전체를 뽑는다.
- **정본은 코드가 아니다.** 값 집합의 정본은 DB 제약 또는 계약 문서이므로, 상수 주석에 그 정본(CHECK 제약명·proto 메시지명)을 적어 어느 쪽이 먼저 바뀌어야 하는지 남긴다.
- **레포에 이미 관용이 있으면 그것을 따른다.** 같은 레포에 두 형태가 섞이는 편이 어느 한쪽을 새로 고르는 것보다 나쁘다. 공용 모듈·형제 서비스에서 같은 종류의 값이 어떻게 표현되는지 먼저 확인한다.

---

## 11. 보고 양식

모든 작업 종료 시, 아래 최소 양식으로 보고한다:

```markdown
### 수정 사항 요약
- (변경한 것 / 왜)

### 사이드 이펙트 / 영향 범위
- (인접 모듈 / 데이터 / API)

### 검증
- 빌드 / 테스트 / 수동 확인 결과
```

---

## 적용 순서

1. **프로젝트 CLAUDE.md / projectConventions** 에 명시된 규칙이 최우선.
2. 프로젝트에 해당 규칙이 없으면 **이 문서의 일반 원칙**을 적용.
3. 둘 다 명시가 없고 판단이 필요하면 **Spec**을 다시 확인하거나 유저에게 질문.

> 이 문서는 프로젝트 컨벤션의 **대체가 아니라 베이스라인**이다.
