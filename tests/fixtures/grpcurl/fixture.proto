syntax = "proto3";
package fixture;
service Audit { rpc Read(Request) returns (Response); }
message Request {
  string value = 1;
  oneof choice { string name = 2; int32 id = 3; }
}
message Response { string value = 1; }
