---
name: request-mm
description: "작업 유형별(생성/수정/검토/디버깅) 단계적 질문을 통해 실행 가능한 Technical Spec을 생성한다. 'API 만들어줘', '스펙 정리해줘', 요구사항이 모호할 때 사용. start-workflow-mm Phase 1에서 자동 호출됨."
allowed-tools: AskUserQuestion, Read, Glob, Grep, Agent
argument-hint: <기능 설명 또는 요청>
user-invocable: true
---

# API 작업 요청 가이드

작업 유형을 먼저 파악한 후, 유형별 맞춤 질문을 통해 실행 가능한 Technical Spec을 생성한다.

## 핵심 철학

- **"What" 보다 "Where & How"**: 어디에 어떻게 얹을지를 명확히 한다.
- **기존 코드 우선**: 반드시 기존 구조를 파악한 후 명세를 작성한다.
- **한 번에 하나씩**: 질문은 한 턴에 하나만. 답변을 기반으로 다음 질문을 구성한다.
- **유추 가능하면 질문하지 않는다**: 코드베이스에서 답을 찾을 수 있으면 건너뛴다.

---

## Phase 1: 작업 유형 선택

`$ARGUMENTS`에서 유형을 유추할 수 있으면 (판별 기준: 만들어줘/추가 → 생성, 바꿔줘/변경 → 수정, 리뷰/검토 → 검토, 에러/버그/안 됨 → 디버깅) 유추 결과를 확인받고 바로 진행한다.

비어있거나 두 개 이상 유형에 걸치면, 반드시 먼저 `AskUserQuestion`으로 질문한다:

```
어떤 작업을 진행하시나요?

1. API 생성 — 새 엔드포인트 추가
2. API 수정 — 기존 API의 동작/필드/로직 변경
3. API 검토 — 기존 API의 품질/컨벤션/성능 리뷰
4. API 디버깅 — 기존 API의 오류/버그 수정
```


---

## Phase 2: 유형별 단계적 질문

유형이 확정되면, 해당 유형의 질문 흐름을 순서대로 진행한다.
**각 단계에서 코드베이스를 탐색하고, 발견한 내용을 공유하면서 질문한다.**

### 공통 체크: 날짜 범위 기반 비즈니스 상태 판정 (해당 시 자동 적용)

요청이 날짜/시간 범위 필드(`startAt`, `endAt`, `validFrom`, `publishFrom`, `expiresAt`, `scheduledAt` 등)를 다루거나,
"예정 / 진행 중 / 종료 / 활성 / 비활성" 같은 상태 표현이 등장하면, **질문하기 전에 코드베이스에서 기존 패턴을 먼저 탐색**한다.

#### 자동 탐색 절차

1. `Grep`으로 프로젝트 내 날짜 기반 상태 함수/enum 패턴을 수집한다:
   ```
   # 상태 분기 키워드
   grep -rn 'start_at\|end_at\|startAt\|endAt\|validFrom\|expiresAt' internal/ --include='*.go'
   # 파생 상태 계산
   grep -rn 'ComputeStatus\|DeriveStatus\|Now()\|time\.Now()' internal/ --include='*.go' | grep -i 'status\|state\|phase'
   # 상태 enum
   grep -rn 'const (\|type .*Status' internal/ --include='*.go' -A 8 | grep -i 'scheduled\|ongoing\|expired\|active\|inactive'
   ```
2. 발견된 패턴을 **분류**한다:
   - **저장형**: DB 컬럼으로 status를 명시 저장하고 배치/트리거로 전이
   - **파생형**: 날짜만 저장하고 조회 시점에 `now` 기준으로 상태를 계산 (`if now < startAt → SCHEDULED` 등)
   - **혼합형**: status 컬럼 + 날짜 범위 동시 관리
3. 경계값 처리 규칙을 찾는다: `startAt` 경계 포함/미포함, `endAt` 경계 포함/미포함, 동일 시각 처리, 타임존.

#### 사용자 제안 (표준 제안 포맷)

탐색 결과가 있으면, **질문 대신 아래 포맷으로 제안**하고 확인만 받는다.

> 코드베이스에서 날짜 범위 상태 패턴을 확인했습니다:
>
> - **형태**: {저장형 / 파생형 / 혼합형}
> - **참조 구현**: `{file:line}` — `{함수/enum명}`
> - **상태 값**: {SCHEDULED / ONGOING / EXPIRED / ...}
> - **경계 규칙**: `startAt <= now < endAt` (start 포함, end 미포함) 등
>
> 이번 기능도 같은 패턴을 따르는 게 맞나요? 다르게 가야 하면 어떤 부분이 다른지 알려주세요.

탐색 결과가 없으면 아래 순서로 질문한다:
1. 상태를 DB에 저장할지(저장형) 조회 시점에 계산할지(파생형)
2. 상태 값 목록
3. 경계 포함/미포함 규칙과 타임존

#### Spec 반영

확정된 규칙은 Phase 4 출력의 **엣지 케이스** 섹션에 아래 케이스를 자동 포함한다 (ID와 참조 구현은 Phase 4에서 전체 표와 함께 부여한다):

| 케이스 | 기대 동작 |
|--------|----------|
| `now == startAt` | 경계 규칙대로 (포함/미포함) |
| `now == endAt` | 경계 규칙대로 |
| `startAt > endAt` | 400 또는 도메인 거부 |
| `startAt == endAt` | 0 길이 처리 방식 명시 |
| 타임존 불일치 요청 | 서버 기준 저장/비교 |


### 유형 A: API 생성

#### Step 1 — 대상 도메인
> "어떤 도메인(리소스)에 대한 API인가요? (예: 상품 옵션, 리뷰, 장바구니)"

- 답변 후 `Glob`/`Grep`으로 해당 도메인의 기존 코드 탐색
- 관련 파일이 있으면: "기존에 `xxx_handler.go`가 있는데, 여기에 추가하는 건가요?"

#### Step 2 — 엔드포인트 설계
> "어떤 엔드포인트가 필요한가요? (Method + Path + 간단한 설명)"
> 예: `POST /v1/reviews 리뷰 생성`, `GET /v1/reviews 리뷰 목록 조회`

#### Step 3 — 핵심 필드
> "Request/Response의 핵심 필드를 알려주세요. (전체가 아니라 비즈니스에 중요한 것만)"
> 예: `name(필수), description, price(필수), categoryId`

- 코드베이스에서 관련 Entity/VO가 있으면 필드를 미리 제시하고 확인

#### Step 4 — 비즈니스 규칙
> "특별한 비즈니스 규칙이 있나요?"
> 예: "재고가 0이면 주문 불가", "관리자만 접근 가능", "하루 5회 제한"

- 없으면 "없음"으로 넘어감

#### Step 5 — DB 실존 데이터 정합성
> "이 API가 참조하는 데이터가 실제 DB에 존재해야 하나요? (FK 관계, 상태값 의존 등)"
> 예: "categoryId가 실제 존재하는 카테고리여야 함", "주문 상태가 PENDING일 때만 취소 가능"

- 코드베이스에서 관련 FK/Unique 제약 조건을 탐색하여 제시
- 존재하지 않는 ID 참조, 삭제된 레코드 참조, 상태 불일치 등 **데이터 정합성 엣지 케이스**를 Spec에 포함

#### Step 6 — DB 변경 여부
> "새 테이블이나 컬럼 추가가 필요한가요? 기존 테이블을 사용하나요?"

- Migration 폴더를 탐색하여 기존 스키마를 확인하고 제시

### 유형 B: API 수정

#### Step 1 — 대상 API 특정
> "어떤 API를 수정하나요? (Method + Path 또는 기능명)"

- 답변 후 해당 Handler/Usecase/Repository 코드를 즉시 탐색
- 현재 동작을 요약하여 제시: "현재 이 API는 ~하고 있습니다"

#### Step 1.5 — 참조 구현 위치
> "수정 대상 코드의 위치를 알고 있나요? (파일:라인 또는 함수명)"

- 유저가 위치를 알면 기록하고, Step 1의 코드 탐색 결과와 대조한다.
- 모르면 Step 1에서 탐색한 결과를 기준으로 자동 특정하여 제시한다.

#### Step 2 — 변경 내용
> "무엇을 바꾸나요?"
> 예: "응답에 `totalPrice` 필드 추가", "정렬 기준 변경", "권한 체크 추가"

#### Step 3 — 영향 범위 확인
> (코드 탐색 결과를 기반으로) "이 변경이 `xxx_usecase.go`와 `xxx_repository.go`에도 영향을 줄 것 같은데, 맞나요?"

- `Grep`으로 해당 함수/타입의 참조를 추적하여 영향 범위를 자동 분석
- 사용자에게 확인만 요청

#### Step 4 — 호환성
> "기존 클라이언트와의 호환성을 유지해야 하나요? (Breaking change 허용 여부)"

### 유형 C: API 검토

#### Step 1 — 검토 대상
> "어떤 API를 검토하나요? (Method + Path, 또는 '최근 변경된 API 전부')"

- `git diff`로 최근 변경사항도 참고 가능

#### Step 2 — 검토 관점
> "어떤 관점에서 검토할까요? (복수 선택 가능)"
> 1. 컨벤션 준수 — 에러 처리, VO 패턴, 네이밍
> 2. 성능 — N+1 쿼리, 인덱스, 불필요한 조회
> 3. 보안 — 인증/인가, 입력 검증, SQL injection
> 4. 전체 (1+2+3)

#### Step 3 — 추가 맥락
> "특별히 신경 써야 할 부분이 있나요?"
> 예: "최근 성능 이슈가 있었음", "외부 서비스 연동 부분 집중"

- 없으면 바로 검토 진행

### 유형 D: API 디버깅

#### Step 1 — 증상
> "어떤 문제가 발생하나요?"
> 예: "500 에러 반환", "응답 데이터 누락", "타임아웃", "잘못된 값 반환"

#### Step 2 — 대상 API
> "어떤 API에서 발생하나요? (Method + Path)"

- 답변 후 해당 API의 전체 흐름 (Handler → Usecase → Repository) 즉시 탐색

#### Step 3 — 재현 조건
> "어떤 조건에서 발생하나요?"
> 예: "특정 ID로 요청할 때만", "데이터가 없을 때", "동시 요청 시"

- 재현에 필요한 최소 조건을 좁혀감

#### Step 4 — 에러 정보
> "에러 메시지나 로그가 있나요? (있으면 공유해주세요)"

- 없으면 코드 흐름 분석으로 가능한 원인을 추론

---

## Phase 3: 코드베이스 탐색

Phase 2의 질문 과정에서 **매 단계마다** 코드베이스를 탐색한다. 질문이 끝난 후 별도로 하지 않는다.

### 탐색 대상
```
CLAUDE.md          → 프로젝트 컨벤션
Handler (rest/)    → API 엔드포인트, Request/Response
Usecase            → 비즈니스 로직, 인터페이스
Repository         → 데이터 접근, Entity
Domain (domain/)   → VO, Command, Error
DI (cmd/setup/)    → 의존성 주입
Errcode            → 에러 코드
Migration          → DB 스키마
```

### 탐색 원칙
- `Glob`으로 파일 검색, `Grep`으로 키워드/타입 검색, `Read`로 내용 확인
- 유사 기능의 **참조 구현**을 식별하여 패턴을 파악
- 발견한 내용은 즉시 사용자에게 공유 (질문과 함께)

---

## Phase 4: Technical Spec 출력

모든 질문이 완료되면, 유형에 맞는 Spec을 출력한다.

### 엣지 케이스 ID 규칙 (엣지 케이스 표를 출력하는 모든 유형 공통)

엣지 케이스 표의 각 행에 **추적 ID**를 부여한다. 후속 스킬(`/minmos-harness:e2e-test-mm`, `start-workflow-mm` Phase 9.8)이 이 ID로 검증 커버리지를 대조하므로, Spec 확정 후 ID를 **재배열하거나 재사용하지 않는다**.

| 항목 | 규칙 |
|------|------|
| 형식 | `EC-01`부터 2자리 순번 (`EC-01`, `EC-02`, …) |
| 부여 단위 | 엣지 케이스 표의 각 행 1개 |
| 부여 시점 | Phase 4에서 전체 표를 구성할 때 일괄 부여 (Phase 2에서 자동 수집된 케이스 포함) |
| 케이스 삭제 시 | 번호를 당기지 않고 해당 행만 제거 (빈 번호 허용) |
| 참조 구현 열 | 같은 상황을 처리하는 **기존 코드**를 `file:line`으로 기재. 없으면 `-` |

**참조 구현 열이 이 규칙의 핵심이다.** Spec의 기대 동작이 프로젝트 기존 컨벤션과 어긋나 있어도(예: 다른 API는 중복 요청에 409를 반환하는데 이번 Spec만 400), 자연어 문장만으로는 유저가 알아채기 어렵다. Phase 3 탐색에서 찾은 참조 구현을 이 열에 남겨두면 Phase 9.8 read-back이 그 이탈을 검출할 수 있다. 참조를 찾지 못했으면 추측해서 채우지 말고 `-`로 둔다.

### 정상 흐름 ID 규칙 (`AC-nn`)

엣지 케이스가 "예외 상황"을 다룬다면, **정상 흐름(Acceptance Criteria)** 은 "성공했을 때 무엇이 보장되는가"를 다룬다.
`start-workflow-mm`의 TDD 단계(Phase 7.1)가 **테스트 근거의 상한**으로 `EC-nn`과 `AC-nn`만 사용하므로, 정상 흐름에도 ID를 부여한다.

| 항목 | 규칙 |
|------|------|
| 형식 | `AC-01`부터 2자리 순번 |
| 부여 단위 | **관측 가능한 결과** 1개 (기능/엔드포인트당 보통 1~2개) |
| 조건 | 외부에서 검증 가능해야 한다 — 응답 status·본문 필드·저장소 상태 변화 등 |
| 삭제 시 | `EC` 와 동일 — 번호를 당기지 않는다 |

"정상적으로 동작한다" 같은 검증 불가능한 문장은 `AC`가 아니다. `201을 반환하고 응답 body에 생성된 id가 포함된다`처럼 **단언 가능한 형태**로 적는다.

### 추적 ID가 없는 Spec (구버전·직접 제공)

유저가 Spec을 직접 제공했거나 구버전 Spec이라 표·ID가 없을 수 있다. 이때 **없는 요구사항을 지어내지 않는다.**

| 상황 | 처리 |
|------|------|
| 표는 없으나 본문에 관측 가능한 조항이 있음 | 그 조항에만 `AC-nn`/`EC-nn`을 부여하고, Spec에 `추적 기준: 본문 조항 기반` 한 줄을 남긴다 |
| 관측 가능한 조항이 하나도 없음 | ID를 부여하지 않고 `추적 기준 없음`으로 명시한다. 후속 스킬은 커버리지 대조를 건너뛴다 |

### 유형 A (생성) — Feature Spec

```markdown
# Feature Spec: [기능명]

## 기능 목표
[한 문장. "~하면 ~해야 한다" 형식]

## 현재 상황 (As-Is)
| 파일 | 역할 | 수정 필요 |
|------|------|----------|
| `path/to/file.go` | [역할] | Yes/No |

### 참조 구현
- `path/to/similar.go` → [이 패턴을 따름]

## 변경 사항 (To-Be)

### 새로 생성할 파일
| 파일 | 용도 |
|------|------|

### 수정할 기존 파일
| 파일 | 변경 내용 |
|------|----------|

### API 엔드포인트
| Method | Path | 설명 | 인증 |
|--------|------|------|------|

### 비즈니스 로직
- 조건 → 동작

## Input/Output

### Request
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|

### Response
| 필드 | 타입 | 설명 |
|------|------|------|

## 제약 사항
- [제약]

## 정상 흐름
| ID | 시나리오 | 보장되는 결과 | HTTP |
|----|---------|--------------|------|
| AC-01 | | | |

## 엣지 케이스
| ID | 케이스 | 기대 동작 | HTTP | 참조 구현 |
|----|--------|----------|------|----------|
| EC-01 | | | | |

## DB 변경 (해당 시)
- [ ] 테이블/컬럼/인덱스

## 구현 체크리스트
- [ ] Domain VO/Command
- [ ] Entity
- [ ] Repository 인터페이스 + 구현
- [ ] UoW 팩토리 메서드
- [ ] Usecase 인터페이스 + 구현
- [ ] Handler + 라우트
- [ ] DI 등록 (cmd/setup/handler.go)
- [ ] 에러 코드 등록 (errcode.go)
- [ ] DDL 마이그레이션 (해당 시)
- [ ] 테스트
```

### 유형 B (수정) — Change Spec

```markdown
# Change Spec: [변경 설명]

## 현재 동작 (As-Is)
[현재 API가 어떻게 동작하는지]

## 변경 후 동작 (To-Be)
[변경 후 어떻게 동작해야 하는지]

## 영향 범위
| 파일 | 변경 내용 | 영향도 |
|------|----------|--------|

## Breaking Changes
- [있으면 기술, 없으면 "없음"]

## 정상 흐름
> 이번 변경으로 **달라지는** 정상 동작만 기재한다.

| ID | 시나리오 | 보장되는 결과 | HTTP |
|----|---------|--------------|------|
| AC-01 | | | |

## 엣지 케이스
> 이번 변경으로 **새로 생기거나 동작이 달라지는** 케이스만 기재한다. 기존 동작이 그대로인 케이스는 제외.

| ID | 케이스 | 기대 동작 | HTTP | 참조 구현 |
|----|--------|----------|------|----------|
| EC-01 | | | | |

## 구현 순서
1. [단계]
2. [단계]
```

### 유형 C (검토) — Review Plan

```markdown
# Review Plan: [대상 API]

## 검토 대상
| Method | Path | 파일 |
|--------|------|------|

## 검토 관점
- [선택된 관점 목록]

## 중점 사항
- [사용자가 언급한 특별 사항]
```

Spec 출력 후 바로 검토를 실행한다.

### 유형 D (디버깅) — Debug Spec

```markdown
# Debug Spec: [증상 요약]

## 증상
[문제 설명]

## 대상 API
[Method] [Path]

## 재현 조건
[조건 정리]

## 재현 케이스
> 버그를 **실패로 드러내는** 케이스. `start-workflow-mm` TDD 단계가 이 표를 재현 테스트의 근거로 사용한다.

| ID | 입력·전제 | 현재 (버그) | 기대 (수정 후) | 참조 구현 |
|----|----------|------------|---------------|----------|
| RC-01 | | | | |

## 코드 흐름 분석
Handler (`file:line`) → Usecase (`file:line`) → Repository (`file:line`)

## 추정 원인
1. [가능성 높은 순서대로]

## 조사 계획
1. [확인할 것]
2. [확인할 것]
```

`RC-nn`은 `EC-nn`·`AC-nn`과 동일한 ID 규칙(재배열·재사용 금지)을 따른다.
**기대 열은 반드시 채운다** — 비어 있으면 재현 테스트가 무엇을 단언해야 할지 정의되지 않는다.

Spec 출력 후 바로 디버깅을 실행한다.

---

## 중요 원칙

1. **질문은 한 턴에 하나** — 여러 질문을 한꺼번에 던지지 않는다.
2. **탐색과 질문을 동시에** — 질문하기 전에 코드를 먼저 보고, 발견한 것을 공유하며 질문한다.
3. **유추 가능하면 건너뛴다** — 코드에서 답을 찾으면 "[코드에서 확인] ~인 것 같은데 맞나요?" 로 확인만 한다.
4. **유추한 내용은 `[Assumption]` 표기** — 사용자에게 확인을 구한다.
5. **불필요한 섹션은 생략** — 해당하는 것만 출력한다.
6. **검토/디버깅은 Spec 출력 후 바로 실행** — Spec은 계획서이자 착수 신호다.
